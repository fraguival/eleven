{% include 'partials/header.njk' %}

<section class="info">
<h1>{{ title }}</h1>
{{ content | safe }}
<ul class="links">
            {%- for post in collections.posts -%}
                <l1><a href="{{ post.url }}">{{ post.data.title }}</a></l1>
            {%- endfor -%}
                
            </ul>
</section>

{% include 'partials/footer.njk' %}